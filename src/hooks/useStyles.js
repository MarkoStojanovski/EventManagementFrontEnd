import { makeStyles } from '@mui/styles';

const useStyles = makeStyles({
  card: {
    maxWidth: 345,
    margin: '15px',
  },
  media: {
    height: 140,
  },
  categoryTitle: {
    textAlign: 'center',
    fontWeight: 'bold',
    marginBottom: '10px',
  },
  description: {
    fontSize: '14px',
    color: 'gray',
  },
});

export default useStyles;
